---
data:
  _extendedDependsOn: []
  _extendedRequiredBy: []
  _extendedVerifiedWith: []
  _isVerificationFailed: false
  _pathExtension: cpp
  _verificationStatusIcon: ':warning:'
  attributes:
    links: []
  bundledCode: "#line 1 \"base.cpp\"\n#include <bits/stdc++.h>\nusing namespace std;\n\
    using ll = long long;\nusing P = pair<int, int>; using Pll = pair<ll, ll>;\nusing\
    \ vi = vector<int>; using vvi = vector<vector<int>>; using vvvi = vector<vvi>;\n\
    using vl = vector<ll>; using vvl = vector<vector<ll>>; using vvvl = vector<vvl>;\n\
    using vb = vector<bool>; using vvb = vector<vector<bool>>;\nusing vp = vector<P>;\
    \ using vpll = vector<Pll>; using vvp = vector<vector<P>>;\nusing vs = vector<string>;\n\
    #define rep(i, s, n) for (long long i = (s); i < (long long)(n); i++)\n#define\
    \ drep(i, s, n) for (long long i = (s); i >= (long long)(n); i--)\ntemplate <typename\
    \ T> bool chmax(T& a, const T& b) { if (a < b) { a = b; return true; } return\
    \ false; }\ntemplate <typename T> bool chmin(T& a, const T& b) { if (a > b) {\
    \ a = b; return true; } return false; }\nconst int INF = 1001001001;\nconst ll\
    \ LLINF = 1e18;\n\nvoid solve()\n{\n    \n}\n\nint main()\n{\n    int t = 1;\n\
    \    //cin >> t;\n    while (t--) solve();\n}\n"
  code: "#include <bits/stdc++.h>\nusing namespace std;\nusing ll = long long;\nusing\
    \ P = pair<int, int>; using Pll = pair<ll, ll>;\nusing vi = vector<int>; using\
    \ vvi = vector<vector<int>>; using vvvi = vector<vvi>;\nusing vl = vector<ll>;\
    \ using vvl = vector<vector<ll>>; using vvvl = vector<vvl>;\nusing vb = vector<bool>;\
    \ using vvb = vector<vector<bool>>;\nusing vp = vector<P>; using vpll = vector<Pll>;\
    \ using vvp = vector<vector<P>>;\nusing vs = vector<string>;\n#define rep(i, s,\
    \ n) for (long long i = (s); i < (long long)(n); i++)\n#define drep(i, s, n) for\
    \ (long long i = (s); i >= (long long)(n); i--)\ntemplate <typename T> bool chmax(T&\
    \ a, const T& b) { if (a < b) { a = b; return true; } return false; }\ntemplate\
    \ <typename T> bool chmin(T& a, const T& b) { if (a > b) { a = b; return true;\
    \ } return false; }\nconst int INF = 1001001001;\nconst ll LLINF = 1e18;\n\nvoid\
    \ solve()\n{\n    \n}\n\nint main()\n{\n    int t = 1;\n    //cin >> t;\n    while\
    \ (t--) solve();\n}"
  dependsOn: []
  isVerificationFile: false
  path: base.cpp
  requiredBy: []
  timestamp: '2023-08-26 19:10:11+09:00'
  verificationStatus: LIBRARY_NO_TESTS
  verifiedWith: []
documentation_of: base.cpp
layout: document
redirect_from:
- /library/base.cpp
- /library/base.cpp.html
title: base.cpp
---
